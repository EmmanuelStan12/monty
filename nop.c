#include "monty.h"

/**
 * nop - this code doesn't do anything
 * @stack: the stack
 * @line_number: line no
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
